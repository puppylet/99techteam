export type Config = {
  apiUrl: string;
  iconBaseUrl: string;
}

export const config: Config = {
  apiUrl: 'https://interview.switcheo.com/prices.json',
  iconBaseUrl: 'https://raw.githubusercontent.com/Switcheo/token-icons/main/tokens/',
}